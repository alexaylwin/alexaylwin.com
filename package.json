{
  "name": "alexaylwin",
  "version": "1.0.0",
  "description": "",
  "main": "gulp.js",
  "scripts": {
    "scss": "node-sass --watch sass -o stylesheets",
    "browser-sync": "browser-sync start --server --index index.html --files \"../alexaylwin.com\"",
    "watch": "concurrently --kill-others \"npm run scss\" \"npm run browser-sync\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap":"4.0.0"
  },
  "devDependencies": {
    "browser-sync": "^2.15.0",
    "concurrently": "^3.5.1",
    "node-sass": "^4.5.3"
  }
}
