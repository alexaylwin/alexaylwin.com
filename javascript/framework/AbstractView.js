var Framework;
(function (Framework) {
    var AbstractView = (function () {
        function AbstractView() {
        }
        return AbstractView;
    }());
    Framework.AbstractView = AbstractView;
})(Framework || (Framework = {}));
